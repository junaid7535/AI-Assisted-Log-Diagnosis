[project]
name = "ai-log-rca-system"
version = "0.1.0"
description = "AI-assisted log diagnosis and root-cause detection system"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
